# B′5 Phase 2 — 사이클 방어 게이트 (97809be)

Claude 독립 검증. `git worktree add --detach` 로 만든 별도 작업 트리에서
`pnpm install --offline --frozen-lockfile` 후 `pnpm gate` 를 1회 실행했다.
실행 전 `lsof -ti :32119` 비점유 확인, 실행 중 소스 변경 없음.

Release gate: PASS

| result | step | time | detail |
| --- | --- | --- | --- |
| ✅ pass | install (frozen lockfile) | 562ms |  |
| ✅ pass | version policy | 220ms |  |
| ✅ pass | lint | 1.8s |  |
| ✅ pass | typecheck | 6.0s |  |
| ✅ pass | unit tests | 11.1s |  |
| ✅ pass | OSV audit (network) | 1.1s |  |
| ✅ pass | web production build | 17.4s |  |
| ✅ pass | playwright chromium | 794ms |  |
| ✅ pass | browser e2e | 166.3s |  |

Chromium 전체 E2E **68/68 PASS (2.8분)**.
단위 테스트: core **171**, web **804**, desktop **72**, scripts **11** = **1,058**.

구현자 측 최종 실행도 소스 고정 상태에서 68/68 이다. 그 이전 실행 1회는
전체 실행 도중 벤치 목적으로 `query.ts` 를 수정해 dev 서버 HMR 이 겹쳤고,
`state-guidance.spec.ts` 의 재삭제 후 힌트 개수 단언이 깨졌다. 소스 고정 재실행에서
같은 테스트가 22.5초에 통과했다. 이 사고에서 규칙 하나를 확정했다.

> **전체 E2E 실행 중에는 소스를 변경하지 않는다.** dev 서버 hot reload 가
> 실행 중간에 끼어들면 그 실행 전체가 검증 증거로서 무효다.

오늘 검증 쪽 원인으로 CI·게이트가 흔들린 네 번째 사례다.
[결정 문서](../decisions/2026-09-07-sequence-cycle-safety.md)에 정책·측정 전문이 있다.
