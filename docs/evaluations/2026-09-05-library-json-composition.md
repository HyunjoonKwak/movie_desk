# 라이브러리 JSON 필드 구성과 파형 분리 — 2026-09-05 (A5)

## 결론

실제 Chrome 152에서 H.264+AAC 1초 클립 200개와 1×1 PNG 800개를 가져온 프로젝트 행은
3,320,659바이트였다. 그중 `waveformPeaks` 하나가 3,079,200바이트(92.7%)였으므로 별도
IndexedDB 프리뷰 저장소로 분리했다. 같은 입력의 후측정 행은 244,659바이트로 92.6% 줄었고,
프로젝트 이름 변경부터 Saved 표시까지는 136ms에서 63ms로 줄었다.

## 방법

- `apps/web/scripts/bench-library.mjs --url http://127.0.0.1:3000 --assets 1000 --videos 200`
- 설치된 Google Chrome의 새 임시 프로필과 개발 서버를 사용했다.
- 선행 벤치의 JavaScript 문자열 길이 대신 최대 프로젝트 행을 UTF-8 바이트로 재고, 각 자산 속성의 JSON key/value 토큰과 구분자 바이트를
  합산했다. `other project data/structure`는 전체 행에서 자산 속성 합계를 뺀 값이다.
- 전후 측정은 같은 Apple Silicon Mac과 같은 세션에서 순서대로 실행했다. 개발 서버 측정이므로
  절대 시간보다 데이터 크기와 큰 폭의 변화에 의미가 있다.

## 분리 전 필드 구성

| 자산 필드 | 바이트 | 전체 행 비중 | 포함 자산 수 |
| --- | ---: | ---: | ---: |
| waveformPeaks | 3,079,200 | 92.7% | 200 |
| opfsPath | 41,800 | 1.3% | 1,000 |
| importedAt | 27,000 | 0.8% | 1,000 |
| capturedAt | 27,000 | 0.8% | 1,000 |
| name | 23,800 | 0.7% | 1,000 |
| id | 20,000 | 0.6% | 1,000 |
| mime | 19,000 | 0.6% | 1,000 |
| durationMs | 18,000 | 0.5% | 1,000 |
| sizeBytes | 15,400 | 0.5% | 1,000 |
| kind | 15,000 | 0.5% | 1,000 |
| height | 11,200 | 0.3% | 1,000 |
| width | 10,400 | 0.3% | 1,000 |
| videoCodec | 5,400 | 0.2% | 200 |
| audioCodec | 5,000 | 0.2% | 200 |
| 기타 프로젝트 데이터/구조 | 2,459 | 0.1% | — |

`filmstripFrames`와 `sourceImageMetadata`는 이 fixture 집합에는 없었다. 필름스트립 이미지 자체는
선행 작업에서 이미 분리됐으며, 이미지 fixture에는 EXIF 원본 메타데이터가 없다.

## 구현 결정

- `movie-desk.previews.v1`의 row kind에 `waveform`을 추가했다. 가져오기·재연결은 파형을 여기 쓰고,
  delete/GC/lease/legacy migration/JSON export 재인라인도 동일한 수명주기를 따른다.
- 메모리 프리뷰 store는 파형을 필요할 때 배치 로드하고 최대 200개 LRU로 제한한다. 타임라인,
  range editor, playhead level meter는 이 store를 소비한다.
- 프로젝트 레코드에는 검색 가능한 작은 `hasAudio?: boolean` 사실을 남긴다. 기존 인라인 파형 레코드는
  migration에서 이 값을 유도한 뒤 파형을 제거하며, `audioPresence`는 이를 우선 사용한다.
- `.movie-desk.json` 내보내기는 이식성을 위해 저장소 파형을 다시 인라인한다.

## 전후 측정

| 항목 | 분리 전 | 분리 후 | 변화 |
| --- | ---: | ---: | ---: |
| 프로젝트 행 JSON | 3,320,659 B | 244,659 B | -92.6% |
| waveformPeaks | 3,079,200 B | 0 B | -100% |
| hasAudio | 0 B | 3,200 B | +3,200 B |
| 가져오기 | 21,014 ms | 8,180 ms | -61.1% |
| 검색 | 162 ms | 65 ms | -59.5% |
| 필터 변경 | 516 ms | 342 ms | -33.7% |
| 새로고침 → 카드 1,000장 | 1,090 ms | 613 ms | -43.8% |
| 이름 변경 → Saved | 136 ms | 63 ms | -54.0% |

후측정에서 가장 큰 필드는 `opfsPath` 41,800바이트(17.1%)였고 나머지는 원본 사실과 사용자
프로젝트 상태다. 다음 파생 데이터 분리 후보가 지배적이지 않으므로 추가 저장소 분리는 권장하지 않는다.
