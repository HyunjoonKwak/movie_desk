# 라이브러리 1,000개 성능 측정 — 2026-09-04 (A5)

## 결론

혼합 자산 1,000개(H.264+AAC 1초 클립 200개, 1×1 PNG 800개)를 실제 Chrome 152에서 가져와
정리·검색·소스 상태 검사·저장·복원을 쟀다. 가져오기(12초)와 새로고침 복원(0.6초)은 처음부터
쓸 만했고, 병목은 **1,000장 카드가 상태 변화마다 전부 다시 렌더되는 것**이었다. 카드를 memo
컴포넌트로 분리하고 화면 밖 카드의 레이아웃·페인트를 건너뛰게 하며(`content-visibility`),
소스 상태 훅이 누락 집합이 바뀔 때만 새 객체를 내도록 고친 뒤 검색 3배, 필터 2.3배,
소스 상태 검사 30배, 편집 후 저장 3.7배 빨라졌다. 후속 가상화로 필터 변경과 메모리 사용량도
줄였고, 파형 저장소 분리까지 포함한 최신 결과를 같은 표에 덧붙였다.

## 방법

- 스크립트: `apps/web/scripts/bench-library.mjs` (Playwright + 설치된 Google Chrome, `next dev` 3000 포트).
  `node apps/web/scripts/bench-library.mjs --assets 1000 --videos 200 --out result.json`
- 측정: 가져오기(파일 선택 → 카드 1,000장), 라이브러리 행 JSON 길이, 검색창 입력 → "n개 중 m개" 갱신,
  길이 필터 변경 → 갱신, 창 포커스로 강제한 소스 상태 검사(`FileSystemFileHandle.getFile` 호출 수로 종료 판정),
  새로고침 → 카드 복원, 프로젝트 이름 변경 → "저장됨" 배지, `performance.memory.usedJSHeapSize`.
- 환경: Apple Silicon Mac, Chrome 152, 개발 서버(turbopack). 프로덕션 빌드는 조금 더 빠를 수 있다.

## 결과

| 항목 | 수정 전 | 카드 최적화 후 | 프리뷰 분리 후 | 가상화 후 |
| --- | --- | --- | --- | --- |
| 가져오기 1,000개 | 13335 ms (13.3 ms/자산) | 11920 ms (11.9 ms/자산) | 10561 ms (10.6 ms/자산) | 7428 ms (7.4 ms/자산) |
| 저장된 프로젝트 JSON | 6.9 MB | 6.9 MB | 3.3 MB | 0.2 MB |
| 검색 키 입력 → 개수 표시 | 166 ms | 54 ms | 53 ms | 37 ms |
| 필터 변경 → 개수 표시 | 804 ms | 346 ms | 345 ms | 48 ms |
| 소스 상태 검사(창 포커스, 1,000개) | 15504 ms · 파일 열기 2917회 | 521 ms · 파일 열기 1000회 | 513 ms · 파일 열기 1000회 | 511 ms · 파일 열기 1000회 |
| 새로고침 → 카드 1,000장¹ | 790 ms | 607 ms | 618 ms | — |
| 새로고침 → 라이브러리 준비² | — | — | — | 510 ms |
| 이름 변경 → 저장됨 배지 | 256 ms | 69 ms | 56 ms | 36 ms |
| JS 힙(가져오기 직후 / 새로고침 뒤) | 243.6 MB / 138.5 MB | 155.2 MB / 233.4 MB | 165.9 MB / 199.9 MB | 116.4 MB / 50.8 MB |
| DOM 카드(가져오기 직후 / 새로고침 뒤) | 1,000 / 1,000 | 1,000 / 1,000 | 1,000 / 1,000 | 16 / 16 |

¹ 이전 세 열은 `[data-asset-card]` 1,000장이 모두 DOM에 돌아올 때까지다. ² 가상화 후 열은 DOM
카드 수가 자산 수와 다르므로 `media-count`가 `1000/1000`이 될 때까지다. 두 행은 정지 조건이
달라 직접 속도 비교에 쓰지 않는다.

프리뷰 분리 후 값은 2026-09-05 같은 Apple Silicon Mac·Chrome 152·개발 서버에서 다시
측정했다. 프로젝트 행에서 `data:image`가 사라져 3.3MB로 줄었지만 파형 등 다른 자산 파생
데이터는 아직 프로젝트 JSON에 남는다. 카드 프리뷰를 가시 영역에서만 읽고 측정 전 보이는 이미지
디코드를 안착시킨 뒤 이름 변경 저장 시간은 56ms로, 분리 전 최적화 값 69ms 수준으로 돌아왔다.

가상화 후 값은 2026-09-06 파형 저장소 분리가 통합된 `main` 위에서 다시 측정했다. 날짜 그룹과
단일 목록을 고정 행 세그먼트로 나누고 IntersectionObserver의 한 화면 여유 안에 든 세그먼트만
마운트했다. 필터 목표(<100ms)는 48ms로 통과했고 DOM 카드는 16장, 새로고침 뒤 힙은 50.8MB로
줄었다. 가져오기도 7.4ms/자산으로 기존 10.6ms/자산 대비 회귀가 없었다. 새로고침은 DOM 카드 수 대신 안정된 전체
개수 표시가 1,000/1,000이 되는 시점으로 바꿔 쟀지만 510ms로 목표(<400ms)에 못 미쳤다. 개발
서버 문서·Yjs 복원 경로가 남은 비용이므로 프로덕션 빌드에서 분리 측정할 후속 항목으로 둔다.
카드 content-box intrinsic 높이와 그리드 border-box 행 높이를 분리한 확인판에서는 페인트된 카드와
스킵된 카드가 섞인 40개 스크롤 높이가 모델과 정확히 일치했다(드리프트 0px). 그룹 헤더도 패널
폭 173/200/240px에서 모두 21px를 유지했다.

수정 전 소스 상태 검사에서 파일 열기가 2,917회로 잰 것은 가져오기 중 시작된 첫 검사가 아직 끝나지
않은 상태에서 포커스 검사가 겹친 탓이다. 검사 자체(1,000회 열기)는 0.5초면 끝나고, 15초는
검사 결과가 8개씩 반영될 때마다 카드 1,000장이 다시 렌더된 비용이었다.

## 남은 병목과 다음 단계

- 필터 변경은 48ms로 해소됐다. 새로고침 준비 510ms는 DOM 카드 수와 무관한 복원·개발 서버
  비용을 프로덕션 빌드에서 나눠 측정한다.
- 저장된 프로젝트 JSON은 프리뷰·파형 분리 후 0.2MB다. 남은 필드 구성은 벤치가 함께 출력한다.
- 힙은 116/51MB로 줄었다. 긴 스크롤 뒤 프리뷰 캐시가 다시 커지는지는 별도로 프로파일링한다.
- 데스크톱 참조 파일에서는 소스 상태 검사 1회가 lease + `media://` 요청이라 브라우저 OPFS보다 비싸다.
  포커스 검사 10초 제한은 유지하되 실기기 도그푸딩에서 다시 잰다.
