node_modules
.pnpm-store
.next
apps/web/.next-*
out
dist
build
coverage
playwright-report
test-results
blob-report
.turbo
.tsbuildinfo
*.tsbuildinfo

# apps/desktop/build/ holds electron-builder asset inputs (icons, dmg
# background) — these are source files, not generated output.
!apps/desktop/build
!apps/desktop/build/**

.env
.env.local
.env.*.local

.DS_Store
*.log
.playwright-mcp/

# Whisper model — fetch via `pnpm --filter @movie-desk/web prebundle:whisper`
apps/web/public/whisper/

# Face Landmarker model — fetched and checksum-verified during desktop builds.
apps/web/public/mediapipe/models/face_landmarker.task
apps/web/public/mediapipe/models/face_landmarker.task.download

# OpenCut clone used as study reference only
reference/

# oh-my-claudecode session state
.omc/
